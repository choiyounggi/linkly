.orchestration/
.worktrees/
.dev-loop/
.claude/tmp/
.claude/worktrees/
__pycache__/
*.pyc
.DS_Store
.venv/
build/
dist/
*.egg-info/
/scale-corpus*/
/graphify-out/

# `uv run` byproduct. This repo builds with hatchling and CI installs with
# pip; uv is only ever a local convenience runner, so a lockfile here would
# declare a toolchain the project does not actually use.
uv.lock
