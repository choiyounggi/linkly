name: Release

# issue #141 phase 1: tag-triggered build + GitHub Release. PyPI publishing is
# explicitly out of scope for this phase (user decision, issue #141) — the
# `publish-pypi` job below is scaffolded but disabled (`if: false`) until a
# PyPI Trusted Publisher is registered for this project (see
# https://docs.pypi.org/trusted-publishers/using-a-publisher/). When that
# happens, also add `environment: pypi` to that job (PyPA recommends gating
# publish jobs behind a protected GitHub Environment) and drop the `if: false`.

on:
  push:
    tags: ["v*"]

jobs:
  gates:
    name: gates (py3.12)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install
        run: python -m pip install --upgrade pip && pip install .

      - name: Test suite (mode B toolchain excluded — issue #141 phase 1)
        run: |
          # impl/tests/test_repo_state.py is excluded here by literal module
          # name, not by filename pattern. Its own module docstring documents
          # "This module never skips, on purpose": it is issue #35's outermost
          # regression and the MLIR/LLVM toolchain is the only thing that
          # exercises its mode-B half, so it must NOT gain a skipUnless guard —
          # that would let the coverage evaporate silently on any machine
          # without the toolchain (see AGENTS.md's note on mode B failures).
          # This runner does not install that toolchain in phase 1 of issue
          # #141, so the module is excluded here instead — loudly, by name.
          # Remove this exclusion once a mode-B toolchain CI job exists.
          echo "excluded from CI discovery (mode B toolchain unavailable, issue #141 phase 1): tests.test_repo_state"
          PYTHONPATH=impl python3 <<'PY'
          import os
          import sys
          import unittest

          EXCLUDED = {"test_repo_state"}
          loader = unittest.TestLoader()
          suite = unittest.TestSuite()
          for fname in sorted(os.listdir("impl/tests")):
              if not (fname.startswith("test_") and fname.endswith(".py")):
                  continue
              module = fname[:-3]
              if module in EXCLUDED:
                  continue
              suite.addTests(loader.loadTestsFromName("tests." + module))

          runner = unittest.TextTestRunner(verbosity=1)
          result = runner.run(suite)
          sys.exit(0 if result.wasSuccessful() else 1)
          PY

      - name: RFC lint
        run: python scripts/rfc_lint.py

      - name: Plugin reference drift check
        run: python scripts/gen_plugin_references.py --check

      - name: Version sync check (issue #141)
        run: python scripts/check_version_sync.py

  build:
    name: build sdist + wheel
    needs: gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install build
        run: python -m pip install --upgrade pip && pip install build

      - name: Build sdist and wheel
        run: python -m build

      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/*
          if-no-files-found: error

  release:
    name: GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      # issue #154: the tag name is untrusted input from outside the repo
      # (github.ref_name). Never interpolate `${{ }}` directly into a `run:`
      # block — GitHub's script-injection hardening guide requires routing
      # such values through an intermediate env var first:
      # https://docs.github.com/en/actions/reference/security/secure-use
      TAG: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Extract release notes from CHANGELOG (issue #154)
        run: |
          python scripts/changelog_section.py "$TAG" > notes.md
          python scripts/changelog_section.py "$TAG" --title > title.txt

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$TAG" dist/* \
            --title "$(cat title.txt)" \
            --notes-file notes.md

  # Scaffolded, disabled: PyPI publishing needs a Trusted Publisher registered
  # at https://pypi.org/manage/project/<name>/settings/publishing/ first
  # (issue #141 explicitly excludes actual publishing from this phase). Once
  # registered, flip `if: false` off and add `environment: pypi` above
  # `runs-on` — PyPA recommends a protected environment for publish jobs.
  publish-pypi:
    name: Publish to PyPI (disabled — issue #141 phase 1)
    if: false
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # required for PyPI Trusted Publishing (OIDC)
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
