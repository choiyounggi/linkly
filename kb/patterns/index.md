# Patterns — 문서 인덱스

재사용 가능한 검증된 구현 패턴.

| 문서 id | load when |
|---------|-----------|
| `patterns-repository-call` | 저장소 접근 step을 쓸 때 / 어떤 동사가 어떤 operation이 되는지 확인할 때 / repository call / authenticate load find read create update delete |
