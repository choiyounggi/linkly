# AntiPatterns — 문서 인덱스

반복 실패로 확인된 회피 대상 패턴.

| 문서 id | load when |
|---------|-----------|
| `antipatterns-unbounded-retry` | 재시도가 멈추지 않을 때 / `retry`를 늘려 문제를 덮으려 할 때 / 데드라인과 재시도가 겹칠 때 / retry / unbounded retry / backoff / deadline |
