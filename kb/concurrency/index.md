# Concurrency — 문서 인덱스

병렬 실행·fan-out/merge·경쟁 상태 회피.

| 문서 id | load when |
|---------|-----------|
| `concurrency-parallel-merge-fanout` | `parallel` 블록을 쓸 때 / 병렬 분기의 실패를 어떻게 다룰지 / 여러 조회를 동시에 하려 할 때 / parallel merge / fan out / concurrent read |
