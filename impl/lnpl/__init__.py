"""LNPL reference implementation — Phase 1 (parser + IR lowering + interpreter).

Clarity over speed: this is the executable form of the RFC suite
(WebAssembly reference-interpreter convention, plan.md D20 artifact 3).
"""

__version__ = "0.1.0"
